# Phase 5F v3: measured partial-review decision

This decision supersedes the v2 requirement to adjudicate all 3,972 in-scope perceptual candidates. It does not change the frozen scan, queue, threshold, datasets, or the v2 contract; **v2 did not pass** and its finalizer must not report a pass. Pair adjudication is permanently stopped. This decision was committed before final-test model inference.

The append-only v2 ledger is frozen at 762 decisions (indices 0–761): 757 `different_image`, five `different_photo_same_observation`, zero `same_source_image`, zero `uncertain`. The 18 candidates matching both perceptual rules were all inspected with no same-source finding. Across the 450 final-test photographs, byte SHA-256 overlap with other populations is zero; recorded metadata leakage touching final test is zero (all 435 metadata findings concern calibration_v1 ↔ calibration_v2 reuse). The 3,210 remaining in-scope candidates contain no both-rule matches, have median pHash distance 10, and 1,594 lie exactly at distance 10. These are measurements of a prioritized, nonrandom prefix and a candidate distribution—not a statistical bound on undetected duplicates.

The remaining 3,210 in-scope candidates and 12,567 outside v2 scope are permanently `not_adjudicated`, never `not_duplicate`. Final-test cross-population independence has 460/1,681 pairs reviewed, 1,221 unreviewed; the gate-evidence independence channel has 249/2,238 reviewed, 1,989 unreviewed. Within-final-test repetition has all 53 reviewed, including five different photos of the same observation and no confirmed same-source image. The two cross-population perceptual-independence labels are `incomplete_by_decision`. Exact-byte and metadata checks passed, but cannot rule out recompressed or altered reuploads.

The frozen 450-image final test may now be run once under a separately precommitted evaluator and reporting rule. Every result must carry `perceptual_independence_incomplete_by_decision` alongside the numeric metrics; it must not be described as having passed v2 perceptual screening. Do not remove, replace, relabel, or exclude images, including the three manually marked unusable final-test images. Do not retune the model or 0.61 threshold from final-test results, rerun inference, or retroactively upgrade the independence label. A confirmed duplication found by other means later would require separate disclosure, not alteration of this frozen result.

The v3 machine-readable decision and evidence bindings are in `training/perceptual_independence_v3_decision.json`. The Terra single-writer task was archived and its three-minute automation deleted before the ledger freeze; no reviewer or timer should resume.
